name: OntoGuard Decision Authorization conformance

on:
  push:
  pull_request:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  conformance:
    strategy:
      fail-fast: false
      matrix:
        python: ["3.11", "3.12", "3.13"]

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: ${{ matrix.python }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test]"

      - name: Adapter tests
        run: pytest -q

      - name: Emit signed TRACE record
        run: |
          export TRACE_PRIVATE_KEY_PEM="$(openssl genpkey -algorithm ED25519)"
          python examples/emit_record.py --out trust-record.json

      - name: TRACE Level 0 conformance
        run: |
          trace-tests verify \
            --record trust-record.json \
            --level 0

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: conformance-ubuntu-py${{ matrix.python }}
          path: trust-record.json
